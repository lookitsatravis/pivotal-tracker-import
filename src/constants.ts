export enum StoryType {
  EPIC = 'epic',
  FEATURE = 'feature',
  CHORE = 'chore',
  BUG = 'bug',
  RELEASE = 'release',
}

export enum TaskStatus {
  COMPLETED = 'completed',
  NOT_COMPLETED = 'not completed',
}

export const DEFAULT_FEATURE_ESTIMATE = 2;

export const DEFAULT_RESOURCE_LABELS = ['api'];
